import { describe, test, expect } from "bun:test"

describe('urlToeTableRecordKey', () => {
    test('Convert full url to table record key', () => {
        const url = 'https://testing.com/path/1?'
    })
})